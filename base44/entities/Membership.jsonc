{
  "name": "Membership",
  "type": "object",
  "properties": {
    "organization": {
      "type": "string"
    },
    "membership_type": {
      "type": "string"
    },
    "role": {
      "type": "string"
    },
    "start_date": {
      "type": "string",
      "format": "date"
    },
    "end_date": {
      "type": "string",
      "format": "date"
    },
    "current": {
      "type": "boolean",
      "default": true
    },
    "source": {
      "type": "string",
      "default": "manual"
    },
    "import_batch_id": {
      "type": "string"
    },
    "source_document_name": {
      "type": "string"
    }
  },
  "required": [
    "organization"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}